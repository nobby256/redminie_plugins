<h3 class="title"><%=l(:label_issue_sub_category_new)%></h3>

<%= labelled_form_for @sub_category, :as => 'issue_sub_category', :url => project_issue_sub_categories_path(@project), :remote => true do |f| %>
<%= render :partial => 'issue_sub_categories/form', :locals => { :f => f } %>
  <p class="buttons">
    <%= submit_tag l(:button_create), :name => nil %>
    <%= submit_tag l(:button_cancel), :name => nil, :onclick => "hideModal(this);", :type => 'button' %>
  </p>
<% end %>
