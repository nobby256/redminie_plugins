<h2><%=l(:label_issue_sub_category_new)%></h2>

<%= labelled_form_for @sub_category, :as => :issue_sub_category,
                      :url => project_issue_sub_categories_path(@project) do |f| %>
<%= render :partial => 'issue_sub_categories/form', :locals => { :f => f } %>
<%= submit_tag l(:button_create) %>
<% end %>
